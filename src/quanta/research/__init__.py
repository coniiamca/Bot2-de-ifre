"""Research core (plan Phase 2–3, first slice): point-in-time panels, a bar-level (Tier-0)
backtest with costs, validation statistics, pre-registration and a trial ledger.

Needs the ``research`` extra (numpy). Nothing on the recording server imports this package:
CLI commands load it lazily.
"""
